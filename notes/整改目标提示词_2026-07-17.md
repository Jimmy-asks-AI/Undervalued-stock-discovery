# 项目整改目标提示词

> 2026-07-18 状态：目标 1—3 已完成并形成可恢复基线。后续执行请使用[后续整改目标提示词](后续整改目标提示词_2026-07-18.md)；其中原目标 4 已按真实依赖拆成 4A—4D，避免在状态源尚未一致时提前重写文档。

适用工作区：`<repo-root>`
依据：[项目全量审查报告](项目全量审查报告_2026-07-17.md)
统一边界：项目当前保持 `research_only / NO_ACTION`。任何目标都不得把工程完成、历史高分、ETF 映射覆盖或探索样本解释为策略有效。

## 推荐执行顺序

| 顺序 | 目标 | 依赖 | 完成后解决什么 |
| --- | --- | --- | --- |
| 1 | 修复资金流前推证据链 | 无 | 消除事后回填伪装成前推证据的 P0 风险 |
| 2 | 建立可恢复的 Git 基线 | 目标 1 | 让当前主线能被提交、克隆、回滚和审计 |
| 3 | 锁定环境并补行为测试 | 目标 1、2 | 把“自检通过”升级为可复现的行为验证 |
| 4 | 重建当前状态与研究治理文档 | 目标 2、3 | 统一 README、日志、brief、实验登记和真实状态 |
| 5 | 修正 PIT 与行业历史宇宙口径 | 目标 3、4 | 明确历史估值和 2022 年后样本的证据边界 |
| 6 | 修复 Dashboard 可信度表达 | 目标 4 | 避免旧数据、移动端缺字段和“刷新”误导 |
| 7 | 结算 7 月 21 日探索样本 | 目标 1；日期不早于 2026-07-21 | 只结算四条探索观察，不推进原始目标 |

目标 1—4 适合连续完成。目标 5 不以“找到新 Alpha”为验收条件；外部 PIT 数据不足时，诚实保持阻断就是合格结果。目标 7 不得提前运行。

---

## 目标 1：修复资金流前推证据链

```text
/goal

在工作区 <repo-root> 中，修复 V5.25—V5.35 资金流前推链的时间完整性、不可变性和晋级门禁。当前存在 P0 风险：观察、候选入场价和全行业基准价可以在计划入场后从历史数据补录，V5.30 不校验这些记录的真实生成时点，V5.31 允许覆盖冻结基线，V5.27 未在结算前强制至少 100 个基准行业，V5.28 最终仍可能把这类样本晋级。

必须处理的文件至少包括：
- scripts/build_v5_25_fund_flow_forward_observer.py
- scripts/build_v5_26_fund_flow_forward_entry_gate.py
- scripts/settle_v5_27_fund_flow_forward_samples.py
- scripts/build_v5_28_fund_flow_promotion_evaluator.py
- scripts/audit_v5_30_fund_flow_forward_ledger_integrity.py
- scripts/build_v5_31_fund_flow_evidence_freeze_manifest.py
- scripts/build_v5_32_fund_flow_holding_observation.py
- scripts/build_v5_33_fund_flow_entry_price_freeze.py
- scripts/build_v5_34_fund_flow_benchmark_entry_freeze.py
- scripts/build_v5_35_fund_flow_waiting_room.py
- logs/v5_25_fund_flow_forward_ledger.csv

实现要求：
1. 使用 A 股交易日历计算计划入场、退出和持有期，禁止继续使用 pandas BDay 代替交易日。
2. 每条观察必须记录 detected_at_utc、evidence_cutoff、entry_cutoff、source_fingerprint、experiment/cohort_id、规则 ID 和生成代码版本。只有 detected_at_utc < entry_cutoff 才可成为 qualified_for_goal=true。
3. 入场后补录的观察或价格只能标记 late_backfill_excluded，永久不得晋级；不能把历史首个价格重新命名为 frozen。
4. 观察账本改为追加式、可校验的哈希链。关键落盘使用临时文件、fsync、os.replace 和进程锁；不得静默覆盖已有有效记录。
5. V5.27 在写回 settled 前，必须要求候选入场价按时冻结、基准至少覆盖 100 个行业、基准包含候选行业、入退场日期精确存在。缺一项保持 pending，不得向后寻找下一可用日期。
6. V5.30 必须校验观察时间、冻结时间、as-of 时间、哈希链、源指纹、基准覆盖和 late backfill；任何时间倒置都失败。
7. V5.31 基线不存在时默认失败。更新冻结规则只能创建新的 cohort 和新基线，保留旧基线，记录操作者、原因和前后哈希；禁止覆盖后立即显示通过。
8. V5.28 必须依赖 V5.30 的当前完整性通过结果和对应 cohort 哈希。存在任何 late_backfill、时间违规或基准不足时，promotion_ready=false、can_claim_strong_rebound_industries=false。
9. 现有四条 2026-06-22 观察永久保持 exploratory_fund_flow_only、qualified_for_goal=false。不得迁移成合格目标样本，不得改变历史收益字段。
10. 当前项目仍保持 research_only、NO_ACTION、auto_execution_allowed=false。

必须新增正式 pytest 行为测试，至少覆盖：入场后登记、入场后补冻结、缺失冻结基线、覆盖基线、基准仅 2/99 个行业、重复追加、并发/中断写入、A 股节假日、探索样本误晋级。要保留并测试本次审查中的反例：recorded_at 和 freeze_at 晚于入场时，完整性审计必须失败。

验收：
- 上述反例从“通过”变为明确失败；
- 现有四条探索样本仍为 qualified_for_goal=false；
- V5.28 合格样本数为 0，不能声称目标完成；
- 新增测试全部通过；现有 12 项主线自检和 Dashboard build 不回归；
- 生成 outputs/audit/fund_flow_forward_chain_remediation/，包含 report.md、run_summary.json、top_candidates.csv 和 debug/；
- 报告列出修改文件、数据迁移规则、反例结果、兼容性和未解决项。

禁止事项：不查看或结算 2026-07-21 退出收益；不修改策略阈值、TopN、晋级收益门槛；不新增 V4/V5 调参版本；不提交、不推送；保留用户现有工作树改动。
```

---

## 目标 2：建立可恢复的 Git 基线

```text
/goal

在工作区 <repo-root> 中，为当前研究与 ETF 辅助主线建立可恢复、可审计的 Git 基线。当前 Git 只跟踪约 33 个文件，大量脚本、配置、schema、brief、日志和前推账本未跟踪；根目录没有可提交的 .gitignore，本机 .git/info/exclude 不能随仓库迁移。现有“Git提交边界”审计只检查 .git/HEAD，必须修正。

先只读盘点并形成边界清单，再实施。不得删除、移动或重写历史研究资产，不得使用 git add -A，不得覆盖用户已有的两个 tracked 修改。

要求：
1. 明确三类边界：必须跟踪的当前主线；只需 manifest+hash 的历史研究档案；必须忽略的缓存、输出、账户私有状态、授权数据、node_modules 和临时文件。
2. 新建可提交的 .gitignore，不再依赖 .git/info/exclude 作为唯一规则。
3. 建立 docs/repository_boundary.md，逐项说明跟踪、忽略和归档理由，以及从零恢复命令。
4. 建立机器可读的 current_mainline_manifest.csv/json，至少包含路径、角色、SHA256、是否必需、重建方式和数据敏感级别。
5. 当前 runner、六角色否决链、配置、schema、测试、依赖锁、前推实验登记和必要账本必须可由 Git 恢复。账户真实状态和授权数据不得误提交。
6. 历史研究脚本不得因为数量大而静默丢弃；为它们生成只读档案清单和哈希，明确哪些暂不进主线仓库。
7. 修正 scripts/audit_etf_assisted_trading_completion.py 的 Git 门禁：检查必需文件是否 tracked、关键源码/配置是否存在未跟踪项、工作树是否有未解释修改、.gitignore 是否存在；仅有 .git/HEAD 不得通过。
8. 在独立临时目录执行一次 git archive 或干净 clone 等价恢复验证，证明当前主线可以启动自检。不得依赖原工作区的未跟踪源码。

验收：
- Git 边界审计不再出现假阳性；
- 干净恢复环境能找到全部当前主线必需文件；
- 缓存、outputs、私有账户和授权数据未进入提交范围；
- 提供 staged 文件清单和未纳入文件清单，两者都写明理由；
- 运行当前主线自检、pytest 和 Dashboard build；
- 生成 notes/git_baseline_remediation_report.md。

Git 操作边界：允许创建 Jimmy-asks-AI/remediation-baseline 分支并分批暂存明确文件；只有所有验收通过后才提交，提交前再次展示 staged diff 范围。禁止推送，禁止清理未跟踪文件，禁止 reset --hard、checkout -- 或任何破坏用户改动的命令。
```

---

## 目标 3：锁定环境并建立真实行为测试

```text
/goal

在工作区 <repo-root> 中，建立可复现的 Python/Node 运行环境和正式行为测试层。当前没有 pyproject.toml、requirements 锁文件或 Python 版本声明；pytest 没有任何测试；12/12 回归主要调用脚本 --self-check，23/23 工程完成度多处只检查源码字符串和输出字段存在性。

要求：
1. 只为“当前主线”声明最小 Python 依赖，区分必需依赖、历史可选依赖和外部数据商可选 SDK。固定 Python 版本范围和当前可复现版本，生成 pyproject.toml 与锁文件；不得把凭证写入仓库。
2. 保留前端 package-lock.json，补充 Node 版本声明，并确保 npm ci、npm run build 可以从干净环境执行。
3. 建立 tests/，把关键门禁从源码字符串检查改为输入输出行为测试。至少覆盖：PIT/as-of、数据陈旧与乱序、网络失败保留最近合格快照、ETF 生命周期、重复前推、时间倒置、基准覆盖不足、停牌/涨跌停/T+1、账户过期、持仓风险、建议后风险复算、自动执行始终关闭。
4. 将“12/12 回归”在文档和报告中准确命名为“自检回归”；新增独立的 behavior_test_pass_count，不得混算。
5. 完成度审计中的源代码字符串检查只能作为契约存在性检查，不能再计作行为验证。报告分别给出 contract、unit、integration、data-quality 和 research-evidence 五层状态。
6. 为 Dashboard 增加 typecheck、lint 或最低限度的数据契约测试；package.json 增加清晰的 test/check 脚本。
7. 测试必须离线可运行；涉及网络的部分使用固定 fixture，另设显式 live smoke test，不能让网络波动决定核心测试结果。

验收命令至少包括：
- 从干净环境安装锁定依赖；
- pytest 全部通过且不再是 0 tests；
- pip check 通过；
- 当前 12 项自检回归通过；
- npm ci、TypeScript 检查、Dashboard build 和前端数据契约测试通过；
- 生成 outputs/test/current_mainline_behavior/ 标准四件套及 notes/reproducibility_and_test_report.md。

禁止事项：不改变策略阈值和研究结论；不靠放宽断言让测试通过；不把 live 网络失败伪装成代码通过；不提交任何密钥、账户数据或授权行情。
```

---

## 目标 4：重建当前状态页与研究治理链

```text
/goal

在工作区 <repo-root> 中，重建文档和研究治理，使任何人只看一页就能知道当前目标、证据、数据截止、门禁、下一步和禁止事项。当前 README 同时承担入口、状态页、版本史和长篇研究报告；version_changelog、research_log、review_correction_log 均明显落后；已有 task brief 审计只检查现有文件是否合法，没有检查每个研究版本是否有完整登记。

要求：
1. 新建 CURRENT_STATUS.md，并尽量由 run_summary.json 自动生成。必须包含：当前日期、数据截止、当前动作、策略/审计/前推版本、冻结规则、合格前推样本数、探索样本数、硬门禁、下一允许动作、明确禁止动作和恢复条件。
2. README 只保留项目定位、当前状态摘要、快速开始、证据边界和文档索引。历史版本长文移入 docs/research_history/ 或等价档案，不丢内容。
3. 补齐 logs/version_changelog.md、logs/research_log.md、logs/review_correction_log.md 到当前状态。凡属事后补录，必须明确标记 retrospective inventory，不得伪装成当时记录或事前登记。
4. 建立 research_version_inventory.csv/json，覆盖 V4.72—V5.35 及当前主线，记录版本、目标、源码、配置、task brief、实验登记、输出 manifest、状态、是否 post_hoc、是否进入当前主线。
5. 升级 task brief 审计：每个影响结论的版本必须同时存在 brief、实验登记或明确 post_hoc 标记、changelog、标准输出 manifest；缺失即失败，不再只审已有文件。
6. 统一术语：当前六 Agent 对外称“六角色确定性否决链”；区分策略版本、研究审计版本、数据治理版本和前推 cohort。
7. 所有显眼位置保持统一结论：research_only、NO_ACTION、强行业 Alpha 未验证、人工辅助交易未就绪、自动交易禁止。
8. README 的快速开始必须覆盖 Python 环境、数据构建、当前 runner、pytest、npm ci/build/preview；端口和实际配置一致。

验收：
- CURRENT_STATUS.md 与当前 run_summary 的关键字段一致；
- README 不再同时把 V4.70“100分”和 production_ready=false 当作两个并列当前结论；
- V4.72—V5.35 治理覆盖缺口全部列出并有明确状态；
- 事后库存与事前预登记能被机器区分；
- 文档内本地链接全部有效；
- 生成 outputs/audit/research_governance_coverage/ 标准四件套。

禁止事项：不补造历史时间戳；不把缺失 brief 自动视为通过；不修改策略结论；不删除历史报告；不把文档整理变成新的策略版本。
```

---

## 目标 5：修正 PIT 估值与行业历史宇宙口径

```text
/goal

在工作区 <repo-root> 中，修正历史估值的 available_date/release-lag 边界和申万行业分类宇宙断点。目标是确认哪些历史结果仍可作为描述性证据，哪些必须降级或阻断；不是继续挖掘新 Alpha。

当前问题：申万历史估值源仍缺真实发布时间和源版本审计，V5.11 存在 trade_date 与 signal_date 等值合并；回测横截面从 2015 年约 68 个行业跃升到 2022 年约 124 个、2023 年末 131 个，而 2022 年又被反复当作样本外区间，已不再是干净留出集。

要求：
1. 为历史估值建立字段级数据合同：trade_date、published_at、available_date、fetched_at、source_version、revision_status。没有可靠发布时间时必须标记 unavailable_for_promotion，不得自行假设同日可见。
2. 使用 A 股交易日历做 available_date 和 as-of merge；不得把当前成分、当前行业分类或当前快照回填历史。
3. 建立行业分类/研究宇宙变化审计，列出每个时期的可用行业数、进入/退出行业、分类口径变化和基准可比性。
4. 对现有关键结论至少重做三套只读稳健性复核：共同存续宇宙、当时有效宇宙、分类调整前后分段。分别报告 RankIC、Top20% 命中率、相对收益、样本数和基准差异。
5. 把 2022 年以后标记为已参与迭代的历史审查集，不再称为独立 OOS。晋级只认 2026-07-12 后事前登记的真实前推样本或将来新增的完全未使用数据。
6. 更新 data_catalog/industry_index_source_inventory.csv、相关数据合同、V5.11 审计和目标完成门禁。若真实发布时间或历史分类数据拿不到，保持 blocked，并列明解除条件。
7. 不新增因子、阈值、TopN 或策略版本；不因复核结果较差而改变研究问题。

验收：
- 任何进入晋级计算的历史估值都能证明 available_date；否则被硬阻断；
- 三种宇宙口径报告完整，可解释 2015/2022/2023 的断点；
- 旧 OOS 标签完成纠正；
- 原始目标仍未完成时明确保持 research_only；
- 生成 outputs/audit/pit_universe_methodology_remediation/ 标准四件套及 notes/pit_universe_methodology_report.md。

允许的合格终局包括“外部数据不足，目标继续 blocked”。禁止用代理字段或当前数据回填来制造通过结果。
```

---

## 目标 6：修复 Dashboard 的可信度表达

```text
/goal

使用 frontend-ui-production-flow，在工作区 <repo-root> 中修复研究 Dashboard 的数据可信度、移动端信息完整性、刷新语义和发布便携性。只改展示和数据合同，不改任何策略、阈值、门禁或研究结论。

必须解决：
1. 首屏展示 dashboard_data.json 的 generated_at、各源截止时间、滞后天数、data_quality_warnings 和总体研究状态。过期、缺失或降级数据必须有明显但克制的提示。
2. 现有“刷新”只会 window.location.reload。将按钮改成准确语义，例如“重新读取本地结果”，并明确真正数据刷新需要运行的命令；除非已经实现受控后端，不得让用户误以为按钮会联网刷新研究数据。
3. 600px 以下不得隐藏估值快照日期、PE、PB、股息率和关键门禁证据。可以改为紧凑卡片或可展开详情，但不能只剩综合分数。
4. 页面固定展示：不构成投资建议、历史回放不代表未来、数据可能延迟、当前需人工确认、自动执行关闭。
5. 去掉 dashboard_data.json 中本机 E:\... 绝对路径，改为相对证据路径或稳定 evidence_id，并保证换目录和子路径部署可用。
6. 修复 Vite base 与 `/data/dashboard_data.json` 的子路径冲突；preview 端口与 README 统一为 4175 或更新全部文档为实际端口。
7. 11.26 MB JSON 改为摘要首载、历史明细按需加载；保持图表与历史成交可用，避免一次性下载全部数据。
8. 增加前端数据契约测试、TypeScript 检查和最小可访问性检查。

验收：
- npm ci、typecheck/test、npm run build 全部通过；
- 1440px 桌面与 390px 移动视口截图验证；
- 无横向溢出、无应用控制台错误；
- 移动端仍能看到估值日期和 PE/PB/股息率；
- 模拟陈旧数据和 data_quality_warnings 时，页面明确显示且不产生 BUY 文案；
- 页面重载与数据刷新语义准确；
- 生成 notes/dashboard_trust_remediation_report.md，并保存验证截图路径。

保持现有 Carbon/IBM Plex 视觉体系，不重做品牌，不增加装饰性图表，不改变 NO_ACTION 结论。
```

---

## 目标 7：2026-07-21 后结算四条探索样本

```text
/goal

前提：当前日期不早于 2026-07-21，且“目标 1：修复资金流前推证据链”已经全部验收通过。若任一前提不成立，停止结算并明确 blocked，不得提前读取退出收益。

在工作区 <repo-root> 中，按冻结口径结算 2026-06-22 生成的四条资金流双正探索观察：保险Ⅱ、白酒Ⅱ、游戏Ⅱ、一般零售。它们只回答“这次资金流双正观察后来怎样”，不能回答“系统是否在低估反弹窗口内选出了更强行业”。

要求：
1. 结算前验证观察账本、入场价冻结、131 行基准入场冻结、代码/基线哈希和修复后的时间完整性审计。先保存只读快照和 SHA256。
2. 四条记录必须永久保持 sample_scope=exploratory_fund_flow_only、qualified_for_goal=false；禁止修改成合格目标样本。
3. 只使用精确计划入场日和退出日的申万二级行业指数价格。缺少精确日期、候选价格或至少 100 个同日基准行业时保持 pending，不得向后寻找价格或使用当前快照回填。
4. 计算行业收益、全行业等权基准、相对收益和 Top20% 排名，但所有统计明确标记 exploratory，不进入原始目标、V5.04 前推 cohort 或任何生产晋级。
5. 运行修复后的 V5.27、V5.28、V5.29、V5.30 和 V5.35。V5.28 的 qualified settled count 必须仍为 0，promotion_ready=false，can_claim_strong_rebound_industries=false。
6. 更新 CURRENT_STATUS.md 和资金流证据日历，只记录事实；不修改规则、阈值、TopN、收益门槛或历史样本。

验收：
- 四条探索样本成功结算或逐条给出不可结算原因；
- 无未来填充、无晚冻结冒充、无基准不足；
- 合格目标样本仍为 0；
- 当前主线仍为 research_only，且不会因这四条结果生成买卖建议；
- 生成 outputs/audit/fund_flow_exploratory_settlement_2026_07_21/ 标准四件套和 notes/fund_flow_exploratory_settlement_2026_07_21.md。

禁止事项：不得提前运行；不得新增候选；不得把结果并入强行业 Alpha；不得因为结果好坏调整后续规则；不得自动更新账户或生成交易指令。
```

---

## 后续条件型目标：授权实施数据与真实前推

以下内容现在不应启动独立开发目标，只在外部状态真实变化后再生成新的 `/goal`：

- 已取得可审计、允许当前用途的历史 Level-1、IOPV、价差和可成交深度数据；
- 2026-07-12 后出现新的、在入场前检测到的独立窗口；
- 冻结规则产生足够的已结算前推样本；
- 当日账户、持仓和执行快照由用户确认。

在这些条件出现前，继续调 V4.70、寻找更高历史分数、用免费网页数据替代授权数据，均不应设为目标。
